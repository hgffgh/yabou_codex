# Godot 4+ specific ignores
.godot/
.import/
export.cfg
export_presets.cfg

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/

# Steam dev-only App ID file (never ship this; a real Steam-shipped build
# either omits it or bundles the real App ID via Steamworks partner tooling)
steam_appid.txt

# macOS
.DS_Store
